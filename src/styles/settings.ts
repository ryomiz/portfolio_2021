// breakpoints
export const breakpoints = {
  md: '768px',
}

// colors
export const colors = {
  crimson: '#dc143c',
  grey: '#ddd',
}

// fonts
export const fonts = {
  libre: "'Libre Baskerville', serif",
  noto: "'Noto Sans JP', sans-serif",
}
